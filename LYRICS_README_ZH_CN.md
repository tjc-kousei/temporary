# 歌词数据正文规范

本指南介绍了歌词 API 中每首赞美诗正文数据的编写规则。

## 1. 数据结构

- **获取来源**: `https://tjckousei.com/hymn/api.php?action=get_all_lyrics`
- **键名**: `[编号].txt`（例如：`1.txt`, `124甲.txt`）
- **使用方式**: 前端会从 API 批量取得数据，并保存到浏览器中重复使用。

## 2. 编写格式

### 章节分隔

使用 `[]`（方括号）包围每一节的标题。这些标题将在操作界面中自动生成对应的按钮。

```text
[1]
第一节歌词内容
[2]
第二节歌词内容
[阿们]
阿们
```

### 注音（Ruby 标签）标记

在汉字后紧跟 `()`（括号）并在其中填写注音，系统将在投影屏幕上自动将其转换为注音格式。

- **编写示例**: `荣耀(róngyào)`
- **显示结果**: 在“荣耀”文字上方显示注音。

### 换行与布局

- 文件内的换行将直接反映在投影屏幕上。
- 所有文本行将自动以“居中对齐”的方式显示。
