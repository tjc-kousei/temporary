# 歌詞資料正文規範

本指南介紹歌詞 API 中每首讚美詩正文資料的編寫規則。

## 1. 資料結構

- **取得來源**: `https://tjckousei.com/hymn/api.php?action=get_all_lyrics`
- **鍵名**: `[編號].txt`（例如：`1.txt`, `124甲.txt`）
- **使用方式**: 前端會從 API 批次取得資料，並保存到瀏覽器中重複使用。

## 2. 編寫格式

### 章節分隔

使用 `[]`（方括號）包圍每一節的標題。這些標題將在操作介面中自動產生對應的按鈕。

```text
[1]
第一節歌詞內容
[2]
第二節歌詞內容
[阿們]
阿們
```

### 注音（Ruby 標籤）標記

在漢字後緊跟 `()`（括號）並在其中填寫注音，系統將在投影螢幕上自動將其轉換為注音格式。

- **編寫範例**: `榮耀(róngyào)`
- **顯示結果**: 在「榮耀」文字上方顯示注音。

### 換行與佈局

- 檔案內的換行將直接反映在投影螢幕上。
- 所有文本行將自動以「居中對齊」的方式顯示。
